//! `BasisServerTests/Compute`: one test binary, one module per C# file. `GpuLz4Experiment` is a
//! recorded measurement (an ILGPU kernel that was not worth shipping), not a test, and is not ported.

mod distance_offload_tests;
mod distance_sweep_tests;
